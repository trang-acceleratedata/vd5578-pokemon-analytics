# Verify: Daily 06:00 UTC Airflow orchestration for Pokemon Analytics

## Certification

verdict: **certified**. Every active requirement revision (R-01@1, R-02@1, R-03@2, R-04@2) and the single Scope-and-impact orchestration row are covered by independent evidence. Native-artifact validators (`validate-airflow-dag.py`, `validate-databuildtooljob.py`) exit 0; the ephemeral DAG run `verify_20260915T065111Z` reached terminal `success` with every task instance green; the paired dbt job's `run_results.json` (job instance `3ee79e48-5644-4282-8fd0-ee9d9704adf9`) is all-green across 2 models + 5 tests. The design-reviewer verdict (APPROVE) from the design gate is preserved below; the code-reviewer verdict (APPROVE) appended below is the fresh audit of the committed items and Plan execution evidence. Proposing `shipping`.

## Coverage

| ID / row | Revision | Requirement / scope text | Acceptance criteria | Evidence |
| --- | --- | --- | --- | --- |
| R-01 | 1 | Domain's orchestration target is Apache Airflow, not the native Data Pipeline | `orchestration/` commits exactly one `.ApacheAirflowJob` item and no `.DataPipeline` | `ls orchestration/` → `pokemon_daily.ApacheAirflowJob/`, `pokemon_daily.DataBuildToolJob/`, `pokemon_dlt_runner.Notebook/` — zero `.DataPipeline` directories present. ADR-0001 fixes Airflow as the domain target. |
| R-02 | 1 | One Airflow DAG runs the committed PokeAPI dlt load then the committed dbt project as a single daily unit; a dbt failure means the day failed | DAG contains `dlt_load` (`MSFabricRunJobOperator`, `job_type="RunNotebook"`) then `dbt_build` (`MSFabricRunJobOperator`, `job_type="DataBuildToolJob"`, no `job_params`), each preceded by a `resolve_*` id-by-name task, plus `record_run` and `workload_succeeded` leaf per ADR-0026; DAG state reflects dbt outcome | `orchestration/pokemon_daily.ApacheAirflowJob/dags/pokemon_daily.py` — grep confirms both operators with declared `job_type`, `dbt_build` carries no `job_params`, `record_run` uses `trigger_rule="all_done"`, `workload_succeeded` is an `EmptyOperator` on default `all_success` and is the workload's terminal leaf (`dbt_build >> [record_run, workload_succeeded]`). Static validator `validate-airflow-dag.py` exit 0 confirms the `recorder-only-leaf`, `dropped-dag-module`, `committed-active-schedule`, and `worker-side-dbt` rules pass. |
| R-03 | 2 | Production cadence is daily at 06:00 UTC; committed DAG carries `schedule=None`, `catchup=False`, `max_active_runs=1`; cadence recorded in design record for customer CI/CD to activate at deployment | See left | DAG source: `schedule=None`, `catchup=False`, `max_active_runs=1` (verified by inspection and validator). Design record `docs/design/pipelines/pokemon_daily.md` §Rerun/Consumers records the intended 06:00 UTC daily cadence for customer CI/CD deployment. |
| R-04 | 2 | End-to-end path proved before shipping: DAG runs green on demand against the ephemeral warehouse, `get-dbtjob-run-evidence.py` (or its OneLake seam) all-green; `bronze.pokemon` and marts model populated; `record_run` best-effort in sandbox | See left | DAG run `verify_20260915T065111Z` state = `success`; all 6 task instances = `success` (including `workload_succeeded`). Dbt job instance `3ee79e48-5644-4282-8fd0-ee9d9704adf9` completed 07:00:24 UTC; `run_results.json` (OneLake path `Output/<jobInstanceId>/target/run_results.json` — the deterministic seam when Airflow invokes the dbt job directly and there is no wrapper Fabric Orchestration Pipeline for `get-dbtjob-run-evidence.py`'s activity-run correlation) — 7 nodes all-green: `stg_pokemon` (success), `pokemon_weight_bands` (success), 5 tests (`pass`). `lakehouse_query`: `bronze.pokemon` = 50 rows; `dbo.pokemon_weight_bands` = 3 rows (marts model materializes to the dbt-fabric target schema `dbo` — no `+schema` override in `dbt_project.yml`; the design record refers to it unqualified as `pokemon_weight_bands`). `record_run` completed cleanly on this run; no token-vs-spn write anomaly manifested. |
| Scope row | orchestration | `pokemon_daily` (`.ApacheAirflowJob` + companion `.DataBuildToolJob` + reused `.Notebook`) — Action `create` | Static validation + ephemeral run per Plan Fabric-Airflow-`create` gates | Both static validators exit 0; identity check `git diff HEAD -- orchestration/*/.platform` empty (no `logicalId` drift, though `create` needs none); DAG run `success`; dbt evidence all-green. |

## Gate results

- **Native artifact validation** (`evaluating-orchestration`, kind: orchestration, Fabric Airflow target):
  - `python3 /data/default-plugins/vibedata-data-engineering/0.81.0/scripts/fabric/validate-airflow-dag.py orchestration/pokemon_daily.ApacheAirflowJob --repo-root .` → exit 0 — `ok: pokemon_daily.ApacheAirflowJob passed static validation`.
  - `python3 /data/default-plugins/vibedata-data-engineering/0.81.0/scripts/fabric/validate-databuildtooljob.py orchestration/pokemon_daily.DataBuildToolJob --repo-root .` → exit 0 — `ok: pokemon_daily.DataBuildToolJob passed static validation`.
  - Invoked-artifact existence: `orchestration/pokemon_dlt_runner.Notebook/` present (`logicalId 3abf750d-259a-4b19-85f6-16ef9b44776b`); `transformation/` present with `stg_pokemon.sql` + `pokemon_weight_bands.sql`.
  - Verdict: **pass**. Tier 1 evidence (exit codes).
- **Ephemeral run evidence** (from `plan.md` `## Execution evidence`, Task 2 · Steps 3–4; re-read, not re-run):
  - DAG run `verify_20260915T065111Z` — terminal state `success`; task instances `resolve_dlt`, `dlt_load`, `resolve_dbt`, `dbt_build`, `workload_succeeded`, `record_run` all `success`.
  - Dbt run evidence via the deterministic OneLake seam — job instance `3ee79e48-5644-4282-8fd0-ee9d9704adf9` `Completed` 06:55:56→07:00:24 UTC; `run_results.json` — dbt 1.11.8, elapsed 7.52s, 7 nodes all-green (2 models `success`, 5 tests `pass`); no duplicate `unique_id`; `get-dbtjob-run-evidence.py` was invoked and correctly reported the Airflow path is outside its pipeline-run activity correlation (no `InvokeDataBuildToolJob` activity because a Fabric Data Pipeline is not the caller here), so the run-verified fallback is `Output/<jobInstanceId>/target/run_results.json` read directly, satisfying the same all-green contract.
  - Row counts against `VD_EPHM_FABRIC_WAREHOUSE_*`: `bronze.pokemon` = 50, `dbo.pokemon_weight_bands` = 3.
  - Verdict: **pass**. Tier 4 (plan-task execution evidence) cross-checked against tier 2 (`run_results.json`) and tier 1 (Airflow REST run state).
- **Lifecycle evidence** — single row is `create`; identity check `git diff HEAD -- orchestration/pokemon_daily.ApacheAirflowJob/.platform orchestration/pokemon_daily.DataBuildToolJob/.platform orchestration/pokemon_dlt_runner.Notebook/.platform` → empty (exit 0). Not a modify, so identity check is advisory; recorded for completeness.
- **Dev-artifact scan** — not applicable to `orchestration` kind (per playbook `## Verify`); skipped explicitly.

Whole-suite classification: **Verified** at tier 6 (Certification with full Coverage and passing Gate results).

## Reviewer verdicts

```json
{
  "verdict": "APPROVE",
  "summary": "Design records for pokemon_daily and ADR-0001 satisfy the resolved Requirement (R-01@1, R-02@1, R-03@2, R-04@2) and every design-reviewer invariant applicable to an orchestration kind. Step 0 ADR-promotion pass identified one bar-clearing decision (adopting the Fabric Apache Airflow Job target for the domain); ADR-0001 exists on disk with status: decided and is cited from the design record's Decisions section. The reviewer contract was applied inline by the designing skill, not dispatched via task, per proposal-chaining.md's inline-reviewer requirement on this runtime; lost independence noted accordingly.",
  "issues": [
    {
      "severity": "info",
      "message": "Reviewer independence trade-off — this verdict was produced inline by the designing skill rather than by a dispatched subagent, because a completed task subagent does not return control to the parent conversation on this runtime (process-contracts.md, proposal-chaining.md). Verdict is the result of a fresh re-read of intent.md, the design record, and ADR-0001, deliberately re-derived without anchoring on the writer's stated assumptions.",
      "location": "designing (inline dispatch)"
    },
    {
      "severity": "info",
      "message": "R-03 and R-04 were amended (v1 → v2) during design authoring to reflect the Airflow-target platform contract (committed-active-schedule rule; sandbox token-vs-spn correlation write). Both amendments are contract-driven, preserve the user's business decisions verbatim, and require re-confirmation at the design stop; intent.md's Change history and Change impact record this. Not a finding — surfaced so the design-stop question names the exact reviewed revision set.",
      "location": "docs/requirement/2026-09-15-new-intent-1-3142244a.md"
    }
  ],
  "next_step": "Advance to the design stop with reviewed set R-01@1, R-02@1, R-03@2, R-04@2 and confirm approval before invoking planning."
}
```

```json
{
  "verdict": "APPROVE",
  "summary": "Fresh code-reviewer audit of the committed pokemon_daily orchestration items and the resolved Plan artifact's Execution evidence. Kind is orchestration only; no dbt mart contract, dlt resource, bronze table, or transformation change is introduced or altered by this intent (both `ingestion/` and `transformation/` are consumed as-committed), so the product-scale contract/Tier-1/schema_contract absolutes do not apply to this diff and are not blocked-for-absence per code-reviewer.md's Scale-scope rule. DAG shape matches R-02: resolve_dlt → dlt_load (MSFabricRunJobOperator, job_type=RunNotebook, job_params=DLT_JOB_PARAMS with literal domain coordinates) → resolve_dbt → dbt_build (MSFabricRunJobOperator, job_type=DataBuildToolJob, no job_params) → [record_run (all_done), workload_succeeded (EmptyOperator, all_success)]. `workload_succeeded` is the workload's terminal leaf so the DAG run reflects dbt outcome; `record_run` is deliberately not a lone leaf (recorder-only-leaf rule). Cadence contract holds: schedule=None, catchup=False, max_active_runs=1 (committed-active-schedule rule). No literal workspace GUID, item id, connection string, or credential in the committed DAG or JSON. airflowRequirements = apache-airflow-providers-microsoft-fabric==0.1.1 only (worker-side-dbt rule). orchestration_support.py lives under plugins/ not dags/ (dropped-dag-module rule). dbt-content.json declares whole-project build with no selector, matching the design record. Deterministic gates independently re-run: both validators exit 0. Plan execution evidence cross-checked against tier-1/2 signals: DAG run success with every task instance success, dbt run_results.json all-green (2 models + 5 tests), bronze.pokemon 50 rows, dbo.pokemon_weight_bands 3 rows. No live external calls in tests. No LEFT JOIN null-path exposure introduced by this intent. Reviewer independence trade-off same as the design gate: applied inline per proposal-chaining.md.",
  "issues": [
    {
      "severity": "info",
      "message": "Reviewer independence trade-off — applied inline by the verifying skill rather than dispatched via task, because a completed subagent does not return control on this runtime (process-contracts.md, proposal-chaining.md). Fresh audit against the artifact revisions on disk at HEAD.",
      "location": "verifying (inline dispatch)"
    },
    {
      "severity": "info",
      "message": "The marts model materializes to `dbo.pokemon_weight_bands` (the dbt-fabric target schema) rather than `marts.pokemon_weight_bands`; `dbt_project.yml` sets no `+schema` for the marts group and the design record references the model unqualified. This is a documentation-only observation about the R-04 acceptance wording, not a defect — the run_results all-green and the row count > 0 satisfy the acceptance criterion at the model level. Filing or amending is at the user's discretion.",
      "location": "transformation/dbt_project.yml"
    }
  ],
  "next_step": "Propose shipping."
}
```

## Approvals

_Populated during `shipping`._
