name: 'pokemon_analytics'
version: '1.0.0'
config-version: 2

profile: 'dbt_fabric'

model-paths: ['models']
analysis-paths: ['analyses']
test-paths: ['tests']
seed-paths: ['seeds']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  pokemon_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table

sources:
  pokemon_analytics:
    +enabled: true
